 Type definitions for js-cookie 2.2
 ```
  Cookies.get('Message')
  Cookies.set('Message', `${date}`, {
        path: '/',
        expires: new Date(Date.now() + 31536000000),
      })
      
      
      ````
      
     #  @types/js-cookie
